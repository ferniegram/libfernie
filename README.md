# libfernie

libfernie is the Ferniegram backend.

## Usage

First, you would need to implement `PlatformApp`. See `platformapp.h` for details, or `platformapp.cpp` in Ferniegram source for an example.

The installation of TDLib should be handled by the application and is not

See Ferniegram for a compelte example of including libfernie in your project.

## TODO

Move LottieItem to a separate project.